services:
  api:
    container_name: labres-api-prod
    image: ghcr.io/ctrlaltelite-devs/labres-manager-server:latest
    ports:
      - "5030:5030"
    env_file:
      - .env
    environment:
      - NODE_ENV=production